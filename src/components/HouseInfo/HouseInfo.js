import React from 'react';
import './HouseInfo.scss';

const HouseInfo = (props) => {
    const {houses} = props;
    console.log(houses);
    return (
        <div>
            
        </div>
    );
};

export default HouseInfo;